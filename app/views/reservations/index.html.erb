<div class="mx-auto">
    <h1 class="font-bold text-5xl pb-8">My reservations</h1>
    <table>
        <tbody>
            <% @reservations.each do |reservation| %>
               <%= render partial: "row", locals: { reservation: reservation } %> 
            <% end %>
        </tbody>
    </table>
</div>